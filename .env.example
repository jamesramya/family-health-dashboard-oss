# Root-level env vars are not used by this project.
# Worker secrets go in worker/.dev.vars (for local dev) or via `wrangler secret put` (for deployed envs).
# See worker/.dev.vars.example and docs/CONFIGURATION.md for the full list.
